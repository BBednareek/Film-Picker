name: filmapp
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  animated_text_kit:
  firebase_core:
  cloud_firestore:
  device_info:
  firebase_database:
  firebase_auth:
  url_launcher:
  email_validator:
  flutter_svg:
  intl:
  package_info:

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

